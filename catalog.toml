# Plugin catalog Noctalia reads when this repo is added as a plugin source.
# Keep it in sync with hypr-keymap/plugin.toml when you bump the version.

[[plugin]]
id = "flopet/hypr-keymap"
name = "Hyprland Keymap"
version = "1.0.0"
author = "flopet"
license = "MIT"
icon = "keyboard"
description = "Your Hyprland binds on a keyboard map, with search and editing."
deprecated = false
plugin_api = 30
tags = ["panel", "bar", "hyprland", "utility"]
dependencies = ["lua", "python3", "hyprctl"]
